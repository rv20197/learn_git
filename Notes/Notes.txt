git config --global user.name "Vatsal Rajgor"
git config --global user.email "rv20197@gmail.com"
git init
git add <full_file_name>
git status
git add *.cpp
git add .
git commit -m "init commit" (-m "init commit")--> It is the message and -m is the message flag)
git commit -a -m "new changes" (It will add and commit the newly changed files)
git rm --cached *.exe (It will remove the file from tracking but not from the filesystem)(If --cached is not used then it will delete from the filesystem also)
